# queue
# FIFO
# enqueue/dequeue

# q.enqueue(a)
# q.enqueue(b)
# q.enqueue(b)
# q.dequeue() => b

# dictionary
# enqueue => O(1)
# dequeue => O(N)

# binary search list: list = [2, 1]
# dictionary<element, count>
list=[(a:1)]
list=[(a:1),(b:1)]
(b:2)  (b:1)
# list.search(element)
list=[(b:2),(a:1)]

(b:1)
list=[(a:1,(b:1)]
       
dic2={}
sendMin
maxCount =0
if count not in dic2:
       dic2[count]= [key]
       if minCount >key:
        maxCount= key
       secmin =minc
else:
       dic2[count] = dic2[couunt].append(key(

dict2 = [1000: [a, b], 900: [c]]
maxCount = 1000         
dequeue
[a, b, c, d, e, f, ..., a1000]
        
# list.search(element.key)
# list sorted by count
index= sll.bisect_left(element,key= lambda)
(b:1)
       (b:2)
key= lambda x:   x.value
        

  (reqest=>restre