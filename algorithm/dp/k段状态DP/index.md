
# 最多分K次
最多K次，每个元素不必全选 股票交易的状态机因为每个状态一定是从上一个状态之后才有的，所以可以用状态机的方式设置状态
[股票交易的状态机模型](最多分K段-状态机DP/股票交易.py)
最多K次，每个元素必须选择 分段选择的时候，在每次都会在前 k-1次得到最佳值情况下进行操作
[分段平均的分段选择](最多分K段-状态机DP/分K段DP.py)

# 必须选K个
必须选K个区间，每个元素不必全选：这里其实也是状态机DP
[必须选择K个区间，有的元素可以不选](必须选择K/k個不相交的子数组/kSubArray.py)
每段的长度固定，使用平衡对顶堆预计算初始cost，再用DP和贪心 一起计算多选一段或者不选当前值的代价转移函数
[必须选K段，有的元素可以不选](分k段/平衡对顶堆.py)

[变化条件情况分K轮，每轮条件不同，每个元素必选](分k段/分段DP顺序交换.py)

[分K段，每个元素必选](分k段/partition-array-to-minimize-xor)