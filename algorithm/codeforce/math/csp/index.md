

在算法和人工智能领域，CSP 代表 约束满足问题（Constraint Satisfaction Problem）。

简单来说，CSP 就是寻找一组对象，这组对象必须满足若干个特定的限制条件（约束）。

区间收缩算法（Interval Propagation），它在约束满足问题（CSP）中是非常经典且完备的处理手段。

这个问题的每个变量有一定的区间范围，同时需要满足所有的区间范围可能值加上是100%， 用这个限制来缩放约束后的上下限：[确定区间可能的值](动态区间约束计算.py)